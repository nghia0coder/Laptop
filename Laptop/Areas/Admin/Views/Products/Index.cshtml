@model IEnumerable<GiayDep.Models.SanPham>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Areas/Admin/Views/Shared/_Layout.cshtml";
}

<h1 class="text-center">Loại sản phẩm</h1>

<p>
    <a class="btn btn-primary" asp-action="Create">Thêm mới</a>
</p>
<div class="table-responsive">
    <table class="table table-striped">
        <thead>
            <tr>
                <th>
                    Tên sản phẩm
                </th>
                <th>
                    Giá
                </th>
                <th>
                    Số lượng
                </th>
                <th>
                    Bảo hành
                </th>
                <th>
                    Khuyến mãi
                </th>
                <th>
                    Hình ảnh
                </th>
                <th>
                    Mô tả
                </th>
                <th>
                    Category
                </th>
                <th>
                    Supplier
                </th>
                <th></th>
            </tr>
        </thead>
        <tbody>
            @foreach (var item in Model)
            {
                <tr>
                    <td>
                        @Html.DisplayFor(modelItem => item.Tensp)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.Dongia)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.Soluong)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.Baohanh)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.Khuyenmai)
                    </td>
                    <td>
                        <img class="thumbnail-image" src="~/Contents/img/@item.Hinhanh1" alt="Product Image" />
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.Description)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.MaloaispNavigation.Idloai)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.ManhaccNavigation.Idnhacc)
                    </td>
                    <td>
                        <a class="btn btn-primary" asp-action="Edit" asp-route-id="@item.Idsp">Chỉnh sửa</a> |
                        <a class="btn btn-info" asp-action="Details" asp-route-id="@item.Idsp">Chi tiết</a> |
                        <a class="btn btn-danger" asp-action="Delete" asp-route-id="@item.Idsp">Xóa</a>
                    </td>
                </tr>
            }
        </tbody>
    </table>
</div>

<style>
    .thumbnail-image {
        max-width: 100px;
        max-height: 100px;
    }
</style>
